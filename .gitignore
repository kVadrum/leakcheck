# Workspace policy — never commit local-only project notes.
CLAUDE.md
STATE.md
SPEC.md
SPEC*.md
0[0-9]-*.md
adrs/

# Claude Code per-project settings + hook tree (local).
.claude/

# Local scratch / editor / OS noise
.DS_Store
*.swp
*.swo
*~

# Local test sandboxes
tmp/

# Logs
*.log

# Local-only leakcheck config (per-clone)
.leakcheckrc.local
